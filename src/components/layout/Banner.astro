---
const { title, pagefindIgnore = false } = Astro.props;
---

<div class="banner-bg" id={title.toLowerCase()} data-pagefind-ignore={pagefindIgnore ? "all" : undefined}>
    <div class="banner">
        <h1 class="hero-h1 underline decoration-crimson"><em>{title}</em></h1>
    </div>
</div>