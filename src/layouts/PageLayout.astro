---
import Layout from "./Layout.astro"
import Banner from "../components/layout/Banner.astro";

const { title, pagefindIgnoreBanner = false } = Astro.props;
---

<Layout>
    <Banner title={title} pagefindIgnore={pagefindIgnoreBanner} />
    <slot />
</Layout>

